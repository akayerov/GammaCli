Информационная система Гамма

1. Краткое описание задачи
Разработать Веб-приложение для контроля направлений пациентов в областную онкобольницу

2. Подробное описание задачи
Веб-приложение должно вводить/отражать направления пациентов в областную онкологическую больницу.
Дополнительно подсвечиваются строки, в которых пациенты "не доехали" до онкобольницы
за определенный срок

11/05/2017 Старт
1. Исходный шаблон взят из картографического приложения, почищен от лишних файлов
В шаблоне использовалась Material UI. В рарабатываемой Gamma предполагаю использовать
библиотеку Ant Disign UI for React.  В библиотеке широкий набор элементов
управления. Итого на FrontEnd получается следующий набор используемых библиотек:
React/Redux/Ant

npm install antd --save

12/05/2017
Работаю над аутентификацией пользователя:
   1. Форма Logn.js ввода имя/пароль и т.п, которая должна отдавть данные на сервер методом
      Post
   2. Блок, отвечающий за показ username/displayname, кнопки login/logout

пакет node-fetch (вызывается из isomorphing-fetch)
Асинхронная выборка - проверить

12-17.05.2017
Наткнулся на проблему. Не получается выполнить аутентификацию. Сервер  (порт 3000)
express-session возвращает cookie сессии, который броузер не сохраняет (сторонние куки? ).
Поскольку куки с connect.sid не передаются на сервер, сервер считает, что аутентификация не прошла

Все работает, если запросы к серверу делать непосредствено из броузера
типа localhost:3000/records ( правда сервер сейчас не выдает форму login).
Ему "подсовываем" данные user/password методом через /login метод Post

Отступаю на исследование аутентификации JWT (не через cookie)
